#set( $dataFields = $entity.getAttributes() )
Ext.define('app.model.${entity.name}', {
    extend: 'Ext.data.Model',
    fields: [
#foreach( $field in $dataFields )
		{name:'$field.formatedName(12).trim()'}#if( $foreach.hasNext ), 
#end
#end

    ],
	idProperty: 'id'
});

