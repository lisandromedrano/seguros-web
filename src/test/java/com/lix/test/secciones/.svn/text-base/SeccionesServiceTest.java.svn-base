package com.lix.test.secciones;

import org.junit.Test;
import org.springframework.beans.factory.annotation.Autowired;

import com.lix.secciones.service.SeccionesService;
import com.lix.test.AbstractTestWithContext;

public class SeccionesServiceTest extends AbstractTestWithContext {
	@Autowired
	SeccionesService seccionesOtherService;

	@Test
	public void testFindAll() {
		// List<Secciones> secciones=seccionesOtherService.findAll();
		// Assert.assertThat("Check DB is empty first", secciones.size(), Matchers.is(0));
		// Secciones m = SeccionesMock.createInstance();
		// seccionesOtherService.save(m);
		// Assert.assertThat(m.getId(), Matchers.notNullValue());
		// secciones = seccionesOtherService.findAll();
		// Assert.assertThat("Check Polizas has been created", secciones.size(), Matchers.is(1));
	}
}
