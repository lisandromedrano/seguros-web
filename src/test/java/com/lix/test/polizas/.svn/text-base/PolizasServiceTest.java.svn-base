package com.lix.test.polizas;

import com.lix.test.AbstractTestWithContext;

public class PolizasServiceTest extends AbstractTestWithContext {
	// @Autowired
	// PolizasService polizasService;
	// @Autowired
	// SeccionesService seccionesService;
	// @Autowired
	// ClientesService clientesService;
	// @Autowired
	// CompaniasService companiasService;
	//
	// @Test
	// public void testFindAll() {
	// List<Polizas> accs = polizasService.findAll();
	// Assert.assertThat("Check DB is empty first", accs.size(), Matchers.is(0));
	// Polizas m = PolizasMock.createInstance();
	// savePoliza(m);
	// Assert.assertThat(m.getId(), Matchers.notNullValue());
	// accs = polizasService.findAll();
	// Assert.assertThat("Check Polizas has been created", accs.size(), Matchers.is(1));
	// }
	// private void savePoliza(Polizas m) {
	// //save first cliente compania tipo poliza
	// clientesService.save(m.getClientes());
	// seccionesService.save(m.getTipoPoliza());
	// companiasService.save(m.getCompanias());
	//
	// polizasService.save(m);
	// }
	// public void testSave(){
	//
	// }
}
